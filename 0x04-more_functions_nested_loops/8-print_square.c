#include "main.h"
/**
 * print_square - prints # size given numbers of times
 * @size: the number # to be printed
 */
void print_square(int size)
{
	int j, i;

	for (i = 0; i < size; i++)
	{
		for (j = 0; j < size; j++)
		{
		_putchar(35);
		}
	_putchar('\n');
	}
}
