#include "main.h"
/**
 * mul - multiples two integer
 * @a: the value to be multiplied
 * @b: the value to be multiplied
 * Return: return the value of c
 */
int mul(int a, int b)
{
	int c = a * b;

	return (c);
}
