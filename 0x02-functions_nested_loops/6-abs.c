#include "main.h"
/**
 * _abs - returns the absolute value of the number starting from zero
 * @i: the number to be computed
 * Return: -i if the number is negative else i
 */
int _abs(int i)
{
	if (i < 0)
		return (-i);

	return (i);

}
