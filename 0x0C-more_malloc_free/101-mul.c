#include "main.h"

int _multi(int argc,char* argv[])
{
	int num1;
	int num2;
	argv[0] = num1;
	argv[1] = num2;
	int mul = num1 * num2;

	if (mul == NULL && argc > 2)
		_putchar(69);
		_putchar(72);
		_putchar(72);
		_putchar(157);
		_putchar(72);
		_putchar('\n');
		return (98);
}
