#include "main.h"

char *string_nconct(char *s1,char *s2, unsigned int n)
{
	char *conca = malloc(sizeof(char) * 3);
	
	*conca[0] = s1;
	*conca[1] = s2;

	if (n == 0)
		s1 = "";
	
	if (n >= s2)

	return (conca);
}
