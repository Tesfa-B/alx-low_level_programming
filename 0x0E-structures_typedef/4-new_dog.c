#include "dog.h"
#include <stdio.h>
#include <stdlib.h>

dog_t *new_dog(char *name, float age,char *owner)
{
	if (*new_dog == NULL)
		return(NULL);

	*name = my_dog->name;
	*age = my_dog->age;
	*owner = my_dog->owner;

	return(0);
}
