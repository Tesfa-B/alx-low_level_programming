#include "dog.h"
#include <stdio.h>
#include <stdlib.h>

dog_t *new_dog(char *name, float age,char *owner)
{
	if (*new_dog == 0)
		return(NULL);

	dog_t->name = name;
	dog_t->age = age;
	dog_t->owner = owner;

	return(0);
}
