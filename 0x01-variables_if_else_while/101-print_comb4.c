#include <stdio.h>
/**
 * main - prints the combination of three digits in ascending order 
 * separated by , and followed by space
 */
int main(void)
{
	int x;
	
	for (x = 0; x < 10; x++)
		putchar(x + '0');
	for (x = 50; x < 60; x++)
		putchar(x + '0');

	putchar(s);
	return (0);
}
