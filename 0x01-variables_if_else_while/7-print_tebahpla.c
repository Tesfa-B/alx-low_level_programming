#include <stdio.h>
/**
 * main - prints lowercase alphabets in reverse
 * Return - if success return 0;
 */
int main (void)
{
	int alphabet;
	alphabet = in reverse;
		putchar("%d", alphabet);
	return(0);
}
