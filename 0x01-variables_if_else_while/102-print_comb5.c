#include <stdio.h>
/**
 * main - prints all combinations of two digits 0 - 99 all numbers are two digit
 * separeted by , followed by space in ascending
 * Return - if success return 0
 */
int main(void)
{
	int digit;
	digit = "combo";
		putchar("%d", digit);
		return (0);
}
