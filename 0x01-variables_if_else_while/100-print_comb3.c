#include <stdio.h>
/**
 * main - prints combination of two digits seprated by , followed by space
 * Return: if success return 0
 */
int main(void)
{
	int x;

	for (x = 0; x < 10; x++)
		putchar(x + '0');	
	return (0);
}
